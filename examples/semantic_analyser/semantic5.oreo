{- Multiple declaration of Procedure 'add'
(language cannot handle function overloading) -}
program Test
begin
    procedure int add(var int x, var int y)
    begin
        return x + y;
    end

    procedure int add(var int a, var int b)
    begin
        return a + b;
    end
end